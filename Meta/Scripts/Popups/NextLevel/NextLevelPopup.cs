using System;
using GameCore.CrossScene.Scripts.UI.Popups;

namespace GameCore.Meta.Scripts.Popups.NextLevel
{
    public class NextLevelPopup : TypeInjectablePopup
    {
        protected override Type popupType => typeof(NextLevelPopup);
        
    }
}